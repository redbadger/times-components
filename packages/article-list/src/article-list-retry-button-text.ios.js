import React from 'react';
import { Text } from "react-native";
import styles from "./styles";

const RetryButtonText = () => (
  <Text style={styles.listErrorButtonText}>Retry</Text>
);

export default RetryButtonText;